// queue for commands to be processed
export const COMMAND_QUEUE_NAME = "{commands}";

// queue for responses to be sent
export const RESPONSE_QUEUE_NAME = "{responses}";

// prefix for internal keys
export const INTERNAL_KEY_PREFIX = "internal";

// available platforms
